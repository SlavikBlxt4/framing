import { View, Text } from 'react-native';
import Fonts from '@/constants/Fonts';

export default function ProfileScreen() {
  return (
    <View style={{ flex: 1, alignItems: 'center', justifyContent: 'center' }}>
      <Text>Perfil funciona</Text>
    </View>
  );
}
